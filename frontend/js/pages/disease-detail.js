import { models } from "../core/api.js";
const details = { heart: { overview: "Heart disease describes conditions affecting the heart and blood vessels. Risk develops through a combination of clinical, lifestyle, and inherited factors.", risks: ["Age", "Blood pressure", "Cholesterol", "Smoking", "Diabetes and family history"], screening: "The GeneCare model estimates whether submitted clinical features resemble lower- or higher-risk patterns in its training data. It does not confirm or exclude a condition." }, diabetes: { overview: "Diabetes is a metabolic condition involving how the body regulates blood glucose. Screening indicators can help identify patterns that may warrant professional review.", risks: ["Glucose level", "Body mass index", "Age", "Family history", "Blood pressure"], screening: "The GeneCare model evaluates eight clinical inputs against patterns learned from the Pima dataset. Its output is an educational probability estimate, not a diagnosis." }, "breast-cancer": { overview: "Breast cancer begins when breast cells grow abnormally. This planned module is not currently available for screening.", risks: ["Age", "Family history", "Inherited gene changes", "Breast density", "Lifestyle and hormonal factors"], screening: "This module is not available. Model selection, validation, explainability, and responsible presentation are still in progress." } };
const key = new URLSearchParams(location.search).get("condition"), model = models.find(item => item.key === key) || models[0], detail = details[model.key]; document.querySelector("#detail-content").innerHTML = `<a class="back" href="diseases.html">← Back to conditions</a><div class="detail-heading"><div><p class="eyebrow">${model.category} health</p><h1>${model.name}</h1><p class="muted">${model.summary}</p></div><span class="status ${model.status === 'Active' ? '' : 'dev'}">${model.status}</span></div><div class="detail-layout"><article class="detail-content"><section><h2>Overview</h2><p>${detail.overview}</p></section><section><h2>Risk Factors</h2><ul>${detail.risks.map(risk => `<li>${risk}</li>`).join("")}</ul></section><section><h2>Understanding Your Screening</h2><p>${detail.screening}</p></section><aside class="disclaimer"><strong>Important Disclaimer</strong><p>GeneCare AI is an educational health-risk screening platform. It is not a diagnostic system and should not replace professional medical advice.</p></aside></article><aside class="panel side-panel"><p class="eyebrow">GeneCare model</p><dl><div><dt>Dataset</dt><dd>${model.dataset}</dd></div><div><dt>Model</dt><dd>${model.model}</dd></div><div><dt>Features</dt><dd>${model.features}</dd></div><div><dt>Explainability</dt><dd>${model.explainer}</dd></div></dl></aside></div>`;

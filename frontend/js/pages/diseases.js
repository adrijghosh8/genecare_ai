import { models } from "../core/api.js";
const grid = document.querySelector("#disease-grid"); function render(query = "") { grid.innerHTML = models.filter(model => (model.name + model.category).toLowerCase().includes(query.toLowerCase())).map(model => `<article class="card disease-card"><span class="status ${model.status === 'Active' ? '' : 'dev'}">${model.status}</span><p class="category">${model.category}</p><h2>${model.name}</h2><p>${model.summary}</p><a href="disease-detail.html?condition=${model.key}">Learn more →</a></article>`).join("") || "<p>No conditions match your search.</p>"; } document.querySelector("#disease-search").addEventListener("input", e => render(e.target.value)); render();

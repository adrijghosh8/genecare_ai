
# GeneCare AI

GeneCare AI health screening workspace using:
- Python
- Flask
- Deterministic metric-based screening indices
- Local username/password accounts with saved assessment history

## Run

```powershell
pip install -r requirements.txt
python app.py
```

Open `http://127.0.0.1:5000`.

The assessment results are educational screening indices, not medical diagnoses.
